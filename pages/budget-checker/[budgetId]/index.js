const BudgetDetails = () => {
  return (
    <>
      <section id="budget-details">
        <h1>Budget Details</h1>
      </section>
    </>
  );
};

export default BudgetDetails;
// did not get to implement this dynamic paging just because I lacked the time. something for the future
// to see the page in the browser you can type http://localhost:3000/budget-checker/1 or anything after the last /
